{
  "scripts": {
    "start": "docusaurus-start",
    "build": "docusaurus-build",
    "deploy": "npm run build && gh-pages -d build/react-promise-tracker -b gh-pages -r https://github.com/Lemoncode/react-promise-tracker"
  },
  "devDependencies": {
    "docusaurus": "^1.7.3",
    "gh-pages": "^2.2.0"
  }
}
